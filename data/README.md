# Data policy

`fixtures` contains tiny synthetic/test data safe for Git. `offline-demo` contains only the bounded replay bundle. Raw SAR, NetCDF and AIS data stay outside Git with manifests, licences and checksums committed where permitted.
