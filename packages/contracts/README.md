# Canonical contracts

Single source of truth for JSON Schema and golden valid/invalid fixtures. Generate or validate Zod/TypeScript and Pydantic/Python representations from the same examples.

Required contracts include `phase1-to-phase2-v1`, `phase2-to-phase3-v1`, unified errors, artifact/layer manifests and `complete-dashboard-v1`.
