# Training

Suggested internal folders: `src/data`, `src/preprocessing`, `src/models`, `src/losses`, `src/metrics`, `configs`, `scripts` and `tests`.

Entry gates: audited dataset, frozen scene-level manifest and deterministic preprocessing. Exit gate: validated checkpoint plus frozen preprocessing and threshold metadata.
