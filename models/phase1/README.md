# Phase 1 model registry

Keep small manifests and model cards in Git. Store large weights in approved artifact storage or Git LFS. An approved package contains weights, preprocessing profile, threshold, model card, evaluation summary and checksums.
