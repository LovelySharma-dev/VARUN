# Phase 1 offline ML workspace

This tree is deliberately outside production services. It owns dataset audit, leakage-free manifests, preprocessing experiments, U-Net training, validation-only threshold selection, locked evaluation and model-package creation.
